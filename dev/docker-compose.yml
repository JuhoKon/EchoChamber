version: '3'

services:
  postgres:
    image: postgres:14-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: echochamber_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres